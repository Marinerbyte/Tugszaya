services:
  - type: web
    name: captain-tugzy-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PORT
        value: 8080
      - key: DISCORD_TOKEN
        sync: false
      - key: GROQ_API_KEY
        sync: false
